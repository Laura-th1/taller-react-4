import React from 'react';

const Resultado = ({ num }) => {
    return (
        <div>
            <h2>&nbsp; El número ingresado es:</h2>
            <p>&nbsp; {num}</p>
        </div>
    );
};

export default Resultado;
